[package]
name = "baregen-difftest"
version.workspace = true
edition.workspace = true
publish = false
description = "Differential property tests for baregen (internal test crate)"

[dependencies]
baregen = { path = "../baregen" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
proptest = "1"
