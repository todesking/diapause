[package]
name = "diapause"
version.workspace = true
edition.workspace = true

[dependencies]
diapause-macro = { path = "../diapause-macro" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
trybuild = "1"
