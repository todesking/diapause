[package]
name = "baregen"
version.workspace = true
edition.workspace = true

[dependencies]
baregen-macro = { path = "../baregen-macro" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
trybuild = "1"
