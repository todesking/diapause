#[diapause::coroutine(yield = u32, resume = u32, fingerprint)]
fn co((b0, b1): (u32, u32), a2: u32) -> u32 {
    let a0: u32 = b0;
    let a1: u32 = b1;
    match ((((8u32, 3u32).1)).wrapping_add(a0)) % 2u32 {
        0u32 if (2u32) < (((a0) % 5u32)) => yield_!(12),
        _ => yield_!(a1),
    };
    if (12u32) < ((((a2) as u64) as u32)) {
        let f0 = |v: u32| -> u32 { v.wrapping_add(10u32) };
        let mut v1: u32 = f0(4u32);
        let n2 = -7i32;
        'l0: {
            match (2) % 3u32 {
                0u32 => yield_!(((-(((6u32) % 16u32) as i32)) as u32)),
                1u32 if ((v1) < ((1u32).wrapping_sub(a2))) && ((v1) % 2u32 == 0u32) => v1,
                _ => yield_!(((-(((a1) % 16u32) as i32)) as u32)),
            };
            let s3: crate::case_002::co::State = crate::case_002::co(((v1) % 16u32), (((diapause_difftest::Pair { x: a2, y: 10 }.y)) % 16u32));
            let mut o4: Option<u32> = yield_all!(s3);
        }
        format!("{:?}", a2);
    } else {
        let r5 = yield_!(a0);
        match (r5) % 3u32 {
            0u32 => 2u32,
            1u32 => a1,
            _ => yield_!((5u32).wrapping_add(6u32)),
        };
    }
    a2
}