#[diapause::coroutine(yield = u32, resume = u32)]
fn co(a0: u32, a1: u32) -> u32 {
    let mut v0: u32 = (((a0, a1).1)).wrapping_mul((3u32).wrapping_mul(a0));
    yield_!((diapause_difftest::Pair { x: (diapause_difftest::Pair { x: 14u32, y: a1 }.y), y: ((-(((10) % 16u32) as i32)) as u32) }.y))
}