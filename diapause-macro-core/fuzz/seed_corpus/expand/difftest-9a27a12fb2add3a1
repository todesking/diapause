#[diapause::coroutine(yield = u32, resume = u32)]
fn co(a0: u32, a1: u32) -> Result<u32, diapause_difftest::Err1> {
    let q0: Result<u32, diapause_difftest::Err2> = Ok(4u32);
    let s1: crate::case_010::co::State = crate::case_010::co(((13u32) % 16u32), (((diapause_difftest::Pair { x: 15u32, y: a1 }.x)) % 16u32));
    yield_all!(s1)
}