#[diapause::coroutine(yield = u32, resume = u32)]
fn co(a0: u32, a1: u32) -> Option<u32> {
    let w0 = &a1;
    yield_!((*w0));
    let mut v1: u32 = (*w0);
    fn g2(v: u32) -> u32 {
        v.wrapping_mul(0u32)
    }
    let mut v3: u32 = g2(((v1) % 6u32));
    Some(yield_!((12u32).wrapping_add(a1)))
}