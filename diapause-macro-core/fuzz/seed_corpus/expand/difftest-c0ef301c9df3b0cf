#[diapause::coroutine(yield = u32, resume = u32)]
fn co(a0: u32, a1: u32) -> Result<u32, diapause_difftest::Err1> {
    let mut c0: u32 = 0u32;
    'l0: while c0 < 3u32 {
        c0 = c0.wrapping_add(1u32);
        let mut v1: u32 = 8;
    }
    let s2: crate::case_012::co::State = crate::case_012::co(((3u32) % 16u32), ((((a0, a0).1)) % 16u32));
    yield_all!(s2)
}