#[diapause::coroutine(yield = u32, resume = u32)]
fn co(a0: u32, a1: u32) {
    let mut o0: Option<u32> = Some(a1);
    let mut c1: u32 = 0u32;
    'l0: while c1 < 3u32 {
        c1 = c1.wrapping_add(1u32);
        let n2 = -8i32;
        if (a1) < (a0) {
            if let Some(p3) = o0 && ((c1) < (((a0) % 3u32))) {
                let mut x4: u32 = p3;
                'l1: while let Some(p5) = o0 {
                    let mut x6: u32 = p5;
                    o0 = if x6 < 3u32 {
                        Some((x6).wrapping_add(1u32))
                    } else {
                        None
                    };
                    let mut v7: u32 = (((10u32) as u64) as u32);
                }
            }
        } else {
        }
    }
    let mut c8: u32 = 0u32;
    'l2: loop {
        if c8 >= 1u32 {
            break 'l2 ();
        }
        c8 = c8.wrapping_add(1u32);
        let r9 = yield_!(((c8, 8).0));
        let mut o10: Option<u32> = Some(9u32);
        let mut o11: Option<u32> = Some(r9);
    }
}