#[diapause::coroutine(yield = u32, resume = u32)]
fn co(a0: u32, a1: u32) {
    let f0 = |v: u32| -> u32 { v.wrapping_add(0u32) };
    let mut v1: u32 = f0(((-(((12u32) % 16u32) as i32)) as u32));
    let mut c2: u32 = 0u32;
    'l0: while c2 < 3u32 {
        c2 = c2.wrapping_add(1u32);
        let mut v3: u32 = (((a1) % 3u32)).wrapping_sub(a0);
        let mut v4: u32 = (diapause_difftest::Pair { x: a0, y: ((-(((1u32) % 16u32) as i32)) as u32) }.y);
    }
    let mut c5: u32 = 0u32;
    'l1: loop {
        if c5 >= 3u32 {
            break 'l1 ();
        }
        c5 = c5.wrapping_add(1u32);
        let mut v6: u32 = if (c5) % 2u32 == 0u32 {
            3u32
        } else {
            ((0) % 4u32)
        };
        match ((((8u32) as u64) as u32)) % 2u32 {
            0u32 if (5u32) % 3u32 == 0u32 => {
                yield_!(c5);
                let mut v7: u32 = (((a0) % 6u32)).wrapping_mul((a0).wrapping_sub(0u32));
            }
            _ => {
            }
        }
    }
}