#[diapause::coroutine(yield = u32, resume = u32)]
fn co(a0: u32, a1: u32) -> u32 {
    if (((a0).wrapping_add(a1)) % 2u32 == 0u32) || ((a0) < (((a1, a1).1))) {
        let mut c0: u32 = 0u32;
        'l0: loop {
            if c0 >= 1u32 {
                break 'l0;
            }
            c0 = c0.wrapping_add(1u32);
            match (0u32) % 2u32 {
                0u32 => {
                    match ((((((5u32) % 2u32)) as u64) as u32)) % 3u32 {
                        0u32 if ((14u32) < (a0)) || ((((-(((c0) % 16u32) as i32)) as u32)) % 2u32 == 0u32) => yield_!((11u32).wrapping_mul(a1)),
                        1u32 => yield_!(a1),
                        _ => yield_!(a0),
                    };
                }
                _ => {
                }
            }
        }
        return a1;
    } else {
    }
    (((-(((15u32) % 16u32) as i32)) as u32)).wrapping_sub((a0).wrapping_mul(7u32))
}