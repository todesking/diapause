#[diapause::coroutine(yield = u32, resume = u32)]
fn co(a0: u32, a1: u32) {
    let mut o0: Option<u32> = Some(11u32);
    let 0u32 = (a1) % 2u32 else {
        let mut c1: u32 = 0u32;
        'l0: while c1 < 1u32 {
            c1 = c1.wrapping_add(1u32);
            let s2: crate::case_006::co::State = crate::case_006::co(((((((c1) as u64) as u32)) % 16u32), (((((a1) as u64) as u32)) % 16u32)), (((11u32).wrapping_mul(a0)) % 16u32));
            let v3: u32 = yield_all!(s2);
            let mut o4: Option<u32> = None;
            let mut v5: u32 = ((7u32, 0u32).0);
        }
        let mut v6: u32 = 3;
        return;
    };
    yield_!((diapause_difftest::Pair { x: (diapause_difftest::Pair { x: 2u32, y: a1 }.y), y: (((a0) as u64) as u32) }.y));
}